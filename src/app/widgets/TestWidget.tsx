import React from 'react';
import { Widget } from '@/common/types';

export interface TestWidgetProps {

}

const TestWidget: Widget = ({}: TestWidgetProps) => {
  return (
    <div>

    </div>
  );
};

export default TestWidget;
