/**
 * How do we detect whether it's on a phone or not? There's probably a good way
 * to do it using Tailwind I bet.
 */

import React from 'react';

export interface WidgetContainerProps {

}

const WidgetContainer = ({}: WidgetContainerProps) => {
  return (
    <div>

    </div>
  );
};

export default WidgetContainer;
